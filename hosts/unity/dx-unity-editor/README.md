# DX Unity Editor Command Center

Source-level only Unity Editor package scaffold: UPM manifest, asmdefs,
menu/window source, typed command plans, source guards, registry metadata, and
metadata-only receipt path are present.

The package does not execute DX, call a live local service, import assets,
create project files, touch the build pipeline, or mutate scenes and assets.

Loaded Unity Editor test project smoke, local-service proof remains deferred,
local-service execution, package tarball/checksum, signing,
Asset Store readiness remains deferred, Asset Store/distribution review,
project mutation/import proof, and release readiness remain deferred.
