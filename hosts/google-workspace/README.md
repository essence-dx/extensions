# Google Workspace Host Adapters

Google Workspace adapters target Apps Script add-ons and cloud-service bridges.
They cannot talk to a local DX process directly, so source scaffolds must use
least-privilege card surfaces and keep OAuth consent, deployment, and
Marketplace review deferred until proof exists.
